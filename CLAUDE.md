@AGENTS.md

# Claude Code adapter

`AGENTS.md` is canonical. Read only nearest local `AGENTS.md` files for directories involved in the task. Existing Reputation-specific skills under `.claude/skills/` remain valid and should be preferred when they match. Cross-model procedures live under `.agents/skills/` and are read on demand. Keep repo/doc scans bounded.
